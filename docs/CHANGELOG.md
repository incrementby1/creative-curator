# Changelog

## 2026-07-27 — Constellation-only production workspace

- Removed guided workspace and saved-session archive from active product, route, client-flow, and HTTP contracts. Removed URLs and endpoints now use normal Not Found behavior.
- Made Brand Constellation sole production journey.
- Documented centered eight-button icon-only desktop dock, hover/focus tooltips, selected-node Inspector connection and sizing operations, and one chronological Undo and Redo history across graph and annotation commands.
- Retained historical `creative_sessions` rows and migration definitions. No runtime route reads or mutates them, and no destructive migration or data conversion is part of removal.
- Replaced documentation tests through RED → GREEN. RED command: `.venv/bin/python -m unittest tests.test_spatial_documentation -v` — 10 run, 4 failures, 0 errors, 0 skips. Earlier focused GREEN command: `.venv/bin/python -m unittest tests.test_spatial_documentation tests.test_ci_workflow -v` — 15 run, 15 passed, 0 failures, 0 errors, 0 skips.
- Follow-up production scan added `docs/README.md` and `docs/DEMO_TUTORIAL.md` to regression coverage, then removed remaining route and saved-session recovery promises. Follow-up RED: 11 tests with 7 failing subtests. Final guard expansion RED: 13 tests with 8 failing variant subtests. That GREEN: 15 of 15 passed.
- Quality-review RED ran 15 documentation tests with 4 failures for migration safety, public-root behavior, and API completeness. Final enum RED ran 16 documentation tests with 1 failure for omitted `acknowledged` challenge state. Current combined GREEN command: `.venv/bin/python -m unittest tests.test_spatial_documentation tests.test_ci_workflow tests.test_production_runtime -v` — 21 run, 21 passed, 0 failures, 0 errors, 0 skips; one existing `StarletteDeprecationWarning` emitted.
- Task 10 full gates: backend 401 run, 394 passed, 7 guarded local-only skips, 0 failures/errors; client unit 24 files and 137 tests passed; lint and TypeScript clean; production build generated 8/8 static pages; Chromium E2E 110/110 passed. Warnings were one backend Starlette deprecation, one unit jsdom navigation diagnostic, repeated Playwright `NO_COLOR`/`FORCE_COLOR` notices, and one expected reduced-motion browser diagnostic. Read-only npm audit reported 4 high and 0 critical/moderate/low/informational vulnerabilities across 593 dependencies. Dry-run remediation offered two transitive brace-expansion updates but left the Next.js chain and proposed an incompatible Next.js 9.3.3 downgrade, so dependency remediation is deferred and disclosed.
- Active scan caught stale `/api/creative/*` prose in `client/README.md`. Regression coverage failed first, then documentation was corrected to project/settings/user proxy routes. Existing evidence covers Paper, Graphite, and Project at 1440×1000 desktop and 390×844 mobile. No remote Supabase, provider, migration, or merge operation ran.
- Fixed the clean-run production-runtime test after CI proved `find_spec` raises when a removed parent package is absent. The red CI run had 393 passes, 7 guarded skips, and 1 error across 401 tests; focused GREEN passed 3 of 3 tests.
- No remote Supabase or provider operation ran.
